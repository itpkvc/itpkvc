<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>วิทยาลัยอาชีวศึกษาภูเก็ต</title>
    <link rel="icon" href="/images/loginLogo.png">
    <link rel='stylesheet' href='/stylesheets/reset.css' />
</head>
<body>
    <%- include('pubNavbar'); -%>
    <div class="container">
        <div class="box_forgot">
            <form  id="formReset" action="/reset_pw" method="POST" >
               <h4 class="head_h4 mt-3 mb-3">กรุณากรอกรหัสผ่านใหม่</h4>
               <div>
                โปรดป้อนกรอกรหัสผ่านใหม่ของท่าน
               </div>
               <div class="mt-3 mb-0">
                <input id="token" type="text" class="form-control" name="token" value="<%= message %>" >
                <input type="password" class="form-control" id="password" oninput="check_pattern()"  name="password"   placeholder="กรุณากรอกรหัสผ่านใหม่ของผ่าน">
                <small id="emailHelp" class="form-text text-muted">หมายเหตุ **กรุณากรอกรหัสผ่านใหม่ของท่าน**</small>
                  <div id="alertBt">
                </div>
               </div>
               <div>
                <input type="password" class="form-control" id="c_password" oninput="check_pw()"  name="c_password"  placeholder="กรุณากรอกรหัสผ่านใหม่ของผ่าน">
                <small id="emailHelp" class="form-text text-muted">หมายเหตุ **กรุณายืนยันรหัสผ่าน**</small>
                <div id="alertPw">
                </div>  
                </div>
               <div class="text-center mt-3">
                    <button type="submit" class="btn btn-primary mr-1" onclick="submit_pw()" >ตกลง</button>
                    <button type="reset" class="btn btn-secondary mr-1" onclick="reset_pw()">ล้างข้อมูล</button>
                    <button class="btn btn-danger">ยกเลิก</button>
               </div>
            </form>
        </div>
    </div>
    <script type="text/javascript">
        var err = '<%= err %>'
         if(err == 'noToken'){
            Swal.fire({
            title: 'เกิดข้อพิดพลาด?',
            text: "กรุณาส่งอีเมล์ใหม่อีกครั้ง!",
            icon: 'warning',
            confirmButtonColor: '#dc143c',
            confirmButtonText: 'ตกลง'
            }).then((result) => {
                if (result) {
                    window.location.href = '/forgot';
                }
            })  
        }
     
     </script> 
  
</body>

</html>